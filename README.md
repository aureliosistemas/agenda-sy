# agenda-sy
Após clonar o projeto execute o arquivo agenda_sy.sql e configure a conexão no arquivo app/config/parameters.
Para iniciar o servidor do symfony basta executar o arquivo runserver, .bat caso use windows, .sh caso linux.

